version: '3.8'
services:
  ldoc-backend:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - NODE_ENV=production
      - ADMIN_EMAIL=jayaramankalidasanbuisness@gmail.com
    restart: unless-stopped
